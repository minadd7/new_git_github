# git add . 은 모든 변경(추가)사항을 staging area 로 이동하는 명령어입니다.
git add .

# git commit 은 staging area 의 변경사항을 확정짓고 commit ID 생성하는 명령어입니다.
git commit -m "message"

# 원격 repo 에 main 브렌치 변경사항(commit ID)을 업로드하는 명령어입니다.
git push origin main